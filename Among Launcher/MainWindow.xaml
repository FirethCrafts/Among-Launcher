<Window x:Class="AmongLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:views="clr-namespace:AmongLauncher.Views"
        Title="Among Launcher" Height="640" Width="980"
        Background="{StaticResource AppBackground}"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="CanResize"
        BorderThickness="1" BorderBrush="{StaticResource GlassBorder}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome GlassFrameThickness="0" CaptionHeight="36"
                            ResizeBorderThickness="6" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Ambient glass layer behind everything -->
        <views:AmbientBackground Grid.RowSpan="2" Grid.ColumnSpan="2"/>

        <!-- Custom title bar -->
        <Border Grid.Row="0" Background="{StaticResource GlassSurfaceStrong}"
                BorderBrush="{StaticResource GlassBorder}" BorderThickness="0,0,0,1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Among Launcher" FontSize="12" FontWeight="SemiBold"
                       Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="16,0,0,0"/>

            <!-- Status badge pill -->
            <Border Grid.Column="1" x:Name="StatusBadge" CornerRadius="14"
                    Background="{StaticResource GlassSurface}" BorderBrush="{StaticResource GlassBorder}" BorderThickness="1"
                    Padding="12,6" Margin="0,0,16,0" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal">
                    <Ellipse x:Name="StatusIndicator" Width="8" Height="8" Fill="{StaticResource TextMuted}" Margin="0,0,8,0"/>
                    <TextBlock x:Name="StatusText" Text="No Game Running" FontSize="12"
                               Foreground="{StaticResource TextMuted}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button x:Name="StopGameButton" Content="STOP"
                            Background="#DC2626" BorderThickness="0"
                            Foreground="#FFFFFF" FontSize="11" FontWeight="SemiBold"
                            Padding="8,4"
                            Visibility="Collapsed"
                            Click="StopGameButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="BtnBorder" Background="{TemplateBinding Background}"
                                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="BtnBorder" Property="Background" Value="#EF4444"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>

            <!-- Window controls -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="&#xE921;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                        Style="{StaticResource TitleBarButton}" Click="Minimize_Click"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                <Button Content="&#xE922;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                        Style="{StaticResource TitleBarButton}" Click="Maximize_Click"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
                <Button Content="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                        Style="{StaticResource CloseButton}" Click="Close_Click"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
            </StackPanel>
        </Grid>
        </Border>

        <!-- Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="76"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border x:Name="Sidebar" Grid.Column="0" Background="{StaticResource GlassSurfaceStrong}"
                    BorderBrush="{StaticResource GlassBorder}" BorderThickness="0,0,1,0">
                <Border.Effect>
                    <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="4" Direction="0" Opacity="0.5"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Home (top) -->
                    <Button x:Name="HomeButton" Grid.Row="0" Tag="MainView"
                            Click="NavButton_Click"
                            Style="{StaticResource NavButton}"
                            Margin="0,18,0,8" ToolTip="Home">
                        <Path Data="{StaticResource HomeIcon}" Stretch="Uniform" Width="24" Height="24"
                              Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                    </Button>

                    <!-- Library (below home) -->
                    <Button x:Name="LibraryButton" Grid.Row="1" Tag="LibraryView"
                            Click="NavButton_Click"
                            Style="{StaticResource NavButton}"
                            Margin="0,0,0,8" ToolTip="Library">
                        <Path Data="{StaticResource LibraryIcon}" Stretch="Uniform" Width="24" Height="24"
                              Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                    </Button>

                    <!-- Spacer -->
                    <StackPanel Grid.Row="2"/>

                    <!-- Settings (above logout) -->
                    <Button x:Name="SettingsButton" Grid.Row="3" Tag="SettingsView"
                            Click="NavButton_Click"
                            Style="{StaticResource NavButton}"
                            Margin="0,0,0,8" ToolTip="Settings">
                        <Path Data="{StaticResource SettingsIcon}" Stretch="Uniform" Width="24" Height="24"
                              Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                    </Button>

                    <!-- Logout (above avatar) -->
                    <Button x:Name="LogoutButton" Grid.Row="4"
                            Click="Logout_Click"
                            Style="{StaticResource NavButton}"
                            Margin="0,0,0,8" ToolTip="Logout">
                        <Path Data="{StaticResource LogoutIcon}" Stretch="Uniform" Width="24" Height="24"
                              Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/>
                    </Button>

                    <!-- Avatar (bottom) -->
                    <Grid Grid.Row="5" Width="40" Height="40" Margin="0,0,0,16">
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#5865F2" BlurRadius="0" ShadowDepth="0" Opacity="0"/>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Trigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Effect.BlurRadius" To="16" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0.6" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.EnterActions>
                                        <Trigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Effect.BlurRadius" To="0" Duration="0:0:0.2"/>
                                                    <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity" To="0" Duration="0:0:0.2"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </Trigger.ExitActions>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Ellipse Fill="#5865F2">
                            <Ellipse.Effect>
                                <DropShadowEffect Color="#5865F2" BlurRadius="14" ShadowDepth="0" Opacity="0.6"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <Image x:Name="SidebarAvatar" Stretch="UniformToFill" Visibility="Collapsed">
                            <Image.Clip>
                                <EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/>
                            </Image.Clip>
                        </Image>
                    </Grid>
                </Grid>
            </Border>

            <!-- Content area -->
            <ContentControl Grid.Column="1" x:Name="ContentArea" Margin="25,25,6,25"/>
        </Grid>

        <!-- Modal Overlay (top layer) -->
        <views:ModalOverlay x:Name="ModalOverlay" Grid.RowSpan="2" Grid.ColumnSpan="2"/>
    </Grid>
</Window>
