<UserControl x:Class="AmongLauncher.Views.UpdateProgressModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="160" d:DesignWidth="400">
    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
            <Border Background="#22222A" CornerRadius="10" Width="44" Height="44"
                    HorizontalAlignment="Left" VerticalAlignment="Top">
                <TextBlock Text="⬇" FontSize="22" Foreground="#22C55E"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock x:Name="StatusText" Text="Downloading..." FontSize="15" FontWeight="SemiBold"
                       Foreground="{StaticResource TextTitle}" Margin="14,0,0,0"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <ProgressBar x:Name="ProgressBar" Height="6" Minimum="0" Maximum="100" Value="0"
                     Foreground="#22C55E" Background="{StaticResource GlassSurfaceWeak}"/>
    </StackPanel>
</UserControl>
