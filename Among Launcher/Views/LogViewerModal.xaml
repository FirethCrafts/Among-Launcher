<UserControl x:Class="AmongLauncher.Views.LogViewerModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="IPC Log" FontSize="14" FontWeight="SemiBold"
                   Foreground="#A1A1AA" Margin="0,0,0,10"/>

        <Border Grid.Row="1" Background="#0B0B0E" BorderBrush="#242429" BorderThickness="1"
                CornerRadius="6" Padding="12">
            <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <TextBlock x:Name="LogContent" Text="No logs yet..."
                           Foreground="#A1A1AA" FontSize="12"
                           FontFamily="Consolas" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Refresh" Click="RefreshButton_Click"
                    Style="{StaticResource SecondaryButton}"
                    Height="32" Padding="16,6" Margin="0,0,8,0"/>
            <Button Content="Clear" Click="ClearButton_Click"
                    Style="{StaticResource SecondaryButton}"
                    Height="32" Padding="16,6"/>
        </StackPanel>
    </Grid>
</UserControl>
