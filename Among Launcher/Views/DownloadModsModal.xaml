<UserControl x:Class="AmongLauncher.Views.DownloadModsModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="450">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" MaxHeight="360">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource GlassSurfaceWeak}" BorderBrush="{StaticResource GlassBorder}"
                                BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,10"
                                SnapsToDevicePixels="True" UseLayoutRounding="True">
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding FileName}" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextTitle}" FontSize="13"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Status}" FontSize="12"
                                               Foreground="{StaticResource TextBody}" Margin="10,0,0,0"
                                               VerticalAlignment="Center"/>
                                </Grid>
                                <ProgressBar Height="4" Margin="0,8,0,0" Minimum="0" Maximum="100"
                                             Value="{Binding Progress, Mode=OneWay}"
                                             Foreground="#10B981" Background="{StaticResource GlassSurfaceWeak}"
                                             Visibility="{Binding IsActive, Converter={StaticResource BoolToVis}}"/>
                            </StackPanel>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)"
                                                             From="1" To="0.55" Duration="0:0:0.7"
                                                             AutoReverse="True" RepeatBehavior="Forever"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBlock Grid.Row="1" x:Name="StatusText" Text="" Foreground="{StaticResource TextBody}" FontSize="12"
                   Margin="0,10,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
