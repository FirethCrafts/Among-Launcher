<UserControl x:Class="AmongLauncher.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="560" d:DesignWidth="680">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Library" Style="{StaticResource PageTitle}"/>

        <Border Grid.Row="1" Style="{StaticResource GlassCard}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="MOD LIBRARY" Style="{StaticResource CardHeader}"/>
                <TextBlock x:Name="LibraryInfoText" Text="Mods here are stored for reuse across profiles."
                           Foreground="{StaticResource TextBody}" TextWrapping="Wrap" FontSize="13"/>
            </StackPanel>
        </Border>

        <!-- Library list -->
        <StackPanel Grid.Row="2">
            <ItemsControl x:Name="LibraryList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource GlassSurfaceWeak}" BorderBrush="{StaticResource GlassBorder}"
                                BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,8"
                                SnapsToDevicePixels="True" UseLayoutRounding="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding FileName}" FontWeight="Bold"
                                               Foreground="{StaticResource TextTitle}" FontSize="13"/>
                                    <TextBlock Text="{Binding Version}" FontSize="11"
                                               Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <Button Grid.Column="1" Content="Install"
                                        Click="InstallLibraryMod_Click"
                                        Style="{StaticResource SecondaryButton}"
                                        Foreground="#10B981"
                                        Height="26" Padding="14,4" FontSize="11"
                                        Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <Button Grid.Column="2" Content="Remove"
                                        Click="RemoveLibraryMod_Click"
                                        Style="{StaticResource SecondaryButton}"
                                        Foreground="{StaticResource AccentBrush}"
                                        Height="26" Padding="14,4" FontSize="11"
                                        VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</UserControl>
