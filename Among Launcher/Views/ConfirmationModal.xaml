<UserControl x:Class="AmongLauncher.Views.ConfirmationModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="450">
    <StackPanel>
        <TextBlock x:Name="MessageText" Text="Are you sure?" FontSize="14"
                   Foreground="{StaticResource TextBody}" TextWrapping="Wrap" Margin="0,0,0,24"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Cancel"
                    Click="CancelButton_Click"
                    Style="{StaticResource SecondaryButton}"
                    Height="36" Padding="20,8" Margin="0,0,12,0"/>
            <Button x:Name="ConfirmButton" Content="Confirm"
                    Click="ConfirmButton_Click"
                    Height="36" Padding="20,8"/>
        </StackPanel>
    </StackPanel>
</UserControl>
