<UserControl x:Class="AmongLauncher.Views.HostControlPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="560" d:DesignWidth="680">
    <UserControl.Resources>
        <!-- GridView header style matching the glass palette -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="{StaticResource GlassSurfaceStrong}"/>
            <Setter Property="Foreground" Value="{StaticResource HeaderBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GlassBorder}"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
        <!-- ListView row style matching the glass theme -->
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBody}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource GlassSurfaceWeak}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#335865F2"/>
                    <Setter Property="Foreground" Value="{StaticResource TextTitle}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Host Control Panel" Style="{StaticResource PageTitle}"/>

        <!-- Lobby card: code + copy + region -->
        <Border Grid.Row="1" Style="{StaticResource GlassCard}">
            <StackPanel>
                <TextBlock Text="LOBBY CODE" Style="{StaticResource CardHeader}"/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="CodeText" FontSize="42" FontWeight="Bold"
                               Foreground="{StaticResource TextTitle}" VerticalAlignment="Center"/>
                    <Button x:Name="CopyCodeButton" Content="COPY"
                            Click="CopyCodeButton_Click"
                            Style="{StaticResource SecondaryButton}"
                            Height="28" Padding="12,4" FontSize="11"
                            Margin="12,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                <TextBlock x:Name="RegionText" FontSize="13"
                           Foreground="{StaticResource TextMuted}" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Players card -->
        <Border Grid.Row="2" Style="{StaticResource GlassCard}">
            <StackPanel>
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="PLAYERS" Style="{StaticResource CardHeader}" Margin="0"/>
                    <TextBlock x:Name="PlayersCountText" Grid.Column="1" Text="0 players"
                               FontSize="12" Foreground="{StaticResource TextMuted}"/>
                </Grid>
                <ListView x:Name="PlayersList" ItemsSource="{Binding Players}"
                          Background="{StaticResource GlassSurfaceWeak}"
                          BorderBrush="{StaticResource GlassBorder}" BorderThickness="1"
                          Foreground="{StaticResource TextBody}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Player" Width="220"
                                            DisplayMemberBinding="{Binding PlayerName}"/>
                            <GridViewColumn Header="Discord" Width="140"
                                            DisplayMemberBinding="{Binding DiscordUserId}"/>
                            <GridViewColumn Header="Host" Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="HOST" FontSize="10" FontWeight="Bold"
                                                   Foreground="#10B981">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsHost}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="" Width="80">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Kick" Click="KickButton_Click"
                                                Height="24" Padding="10,4" FontSize="11">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsHost}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="RePostButton" Content="RE-POST"
                    Click="RePostButton_Click"
                    Style="{StaticResource SecondaryButton}"
                    Margin="0,0,12,0"/>
            <Button x:Name="DisbandButton" Content="DISBAND"
                    Click="DisbandButton_Click"
                    Style="{StaticResource StopButton}"/>
        </StackPanel>
    </Grid>
</UserControl>
