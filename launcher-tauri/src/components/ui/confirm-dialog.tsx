/*
 * ConfirmDialog primitive — alias of `ConfirmModal` (same implementation).
 * Import from here in new views for the `ui/` naming convention.
 */
export { ConfirmModal as ConfirmDialog } from "@/components/Modal";
export type { ConfirmModalProps as ConfirmDialogProps } from "@/components/Modal";
